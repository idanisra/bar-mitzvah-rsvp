import { Box } from '@mui/material';

const HeroSection = () => {
  return (
    <Box 
      sx={{
        minHeight: '25vh',
        display: 'flex',
        alignItems: 'center',
        justifyContent: 'center',
        position: 'relative',
      }}
    >
      {/* Empty hero section - title moved to top bar */}
    </Box>
  );
};

export default HeroSection;
